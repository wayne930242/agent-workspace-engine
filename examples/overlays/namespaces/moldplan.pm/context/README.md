# MoldPlan PM Namespace Overlay

This directory represents namespace-specific files that are layered into the built workspace.

- `config/runtime.env` carries runtime-scoped environment metadata.
- Additional prompts, policies, or templates can be placed here later.
